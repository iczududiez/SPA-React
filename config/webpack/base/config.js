const path = require('path');

module.exports = {
  rootDir: path.resolve(__dirname, '../../../'),
  srcDir: path.resolve(__dirname, '../../../src/'),
  entryDir: path.resolve('./src'),
  outputDir: path.resolve('./dist'),
  configDir: path.resolve('./config'),
};
